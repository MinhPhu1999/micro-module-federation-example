import type { ReactNode } from 'react';
import '../index.css';
interface ProtectedRouteProps {
    children: ReactNode;
}
export declare const ProtectedRoute: ({ children }: ProtectedRouteProps) => import("react").JSX.Element;
export {};
